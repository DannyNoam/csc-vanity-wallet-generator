spring:
  datasource:
    url: jdbc:mariadb://csc-vanity-wallet-db:3306/wallet-db
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Password1}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none